"""
git config --global user.name "Rohanskadu7309"
git config --global user.email "rohanskadu7309@gmai.com"
git config --global color.ui auto
git init
git remote add origin AddGitRepoHere

git pull origin main
git status
git add .
git commit -m "add commit message"
git push origin main

to change name of brnach : git branch -m master main
to merge in another brnach : git push -u origin master:main

to set upstream : git push --set-upstream origin main

to create new branch: git checkout -b branch_name
"""